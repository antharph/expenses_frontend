package com.example.expenses_frontend

import io.flutter.embedding.android.FlutterActivity

/**
 * Flutter renders Register, Login, and Dashboard; HTTP calls use Dio in Dart with
 * `API_URL` from `.env` (see `.env.example`). Native constants live in [ApiConfig].
 */
class MainActivity : FlutterActivity()

package com.maiexpenses.app

import io.flutter.embedding.android.FlutterActivity

/**
 * Flutter renders Register, Login, Dashboard, and Expenses; HTTP calls use Dio in Dart with
 * `API_URL` from `.config/*.json` (dart-define). Native path reference: [ApiConfig] (`/api/v1/expenses`, etc.).
 */
class MainActivity : FlutterActivity()
